name: example
description: Example
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  code_text_field:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/settings/autoRefactoringSettings.json
    - assets/settings/blockSettings.json
